[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = ['npm install --omit=dev']

[phases.build]
cmds = ['echo "No build step required for this project"']

[start]
cmd = 'npm start'